- 2026-07-12 (skeptic-storycard): Verified minor finding at js/storycard.js:204 — ImageBitmap close() loop runs only on render()'s success path; any throw in the draw pipeline after loadPhotos() (e.g. null 2d context from tapePatternCanvas() under canvas-memory pressure) skips it and renderStoryCard's catch falls to renderMinimal() without closing, leaking up to 4 decoded bitmaps per failed attempt until GC. Claim's CSS-token trigger refuted (invalid fillStyle is silently ignored; addColorStop inputs are always parseColor-sanitized), but the structural leak is real. Verdict: real=true; fix is a try/finally around the draw pipeline.
