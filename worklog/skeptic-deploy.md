- 2026-07-12 — Skeptic pass on deploy finding (wrangler.toml:6 `pages_build_output_dir = "."`): CONFIRMED real. Repo-root deploy uploads ARCHITECTURE.md, WORKLOG.md, worklog/ (19 files, more than the 15 claimed), supabase/schema.sql, legacy/wayfarer-v1.html, SETUP*.md, README.md, wrangler.toml as site assets; no .assetsignore/_routes.json/_headers exists. _middleware.js serves any path via next() to valid sessions, so password holders can fetch e.g. /worklog/cloudflare-gate.md. Gated, so minor severity as stated.
- 2026-07-12 — Skeptic pass on deploy finding (.dev.vars exposure via `pages_build_output_dir = "."` + SETUP-CLOUDFLARE.md:59): CONFIRMED real, with one detail refuted. Verified against wrangler 4.110.0 source (src/pages/validate.ts): Pages deploy's only exclusions are the hardcoded minimatch list `_worker.js, _redirects, _headers, _routes.json, functions, **/.DS_Store, **/node_modules, **/.git, .wrangler` — `.dev.vars` is NOT excluded, and Pages deploy has no user-configurable ignore (`.assetsignore` is Workers-assets-only; .gitignore not consulted). So a `.dev.vars` created for the doc's `pages dev` preview (the standard mechanism for supplying SITE_PASSWORD, read from the wrangler.toml dir = repo root) is uploaded by `wrangler pages deploy .` and served at /.dev.vars behind the gate. Refuted detail: `.wrangler/` IS excluded in wrangler@4, so that half of the claim is stale. Worse: repo .gitignore covers only .DS_Store, so `.dev.vars` would also be committed. Doc fix (name the mechanism, warn to delete/exclude, or move site into a subdir) is warranted.
