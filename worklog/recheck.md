- 2026-07-12 — Re-checked all 13 prior review findings against current code; every one is fixed. sanitizeTo now rejects control chars/backslashes (open redirect closed); /logout requires POST (GET shows a confirm page); openShare re-checks the double-open guard after await getEntry; storycard closes ImageBitmaps in a finally block, ellipsizes the dateline against maxW, and loadPhotos walks all photoIds until 4 decode; .bk-iconbtn dropped resting opacity 0.6 (full-opacity --ink-soft ~5.4:1); .jr-edit scrim raised to rgba(20,12,4,0.62); share buttons interpolate entry titles into aria-labels (journal.js:348, book.js:210); wrangler.toml deploys staged dist/ (not repo root) and SETUP-CLOUDFLARE.md documents the staging step plus .dev.vars/.wrangler warnings; SETUP.md step 4 now points Supabase URLs at the Pages domain; session cookie Secure flag derived from request protocol for the http localhost preview. Returned empty findings list.
