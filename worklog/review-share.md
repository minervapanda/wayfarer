## 2026-07-12 — adversarial review: share feature (read-only)

- Reviewed js/storycard.js, js/sharecard.js, share wiring in js/book.js / js/journal.js, css/share.css, share-modal markup in index.html, plus css/book.css / css/journal.css / css/base.css for the new buttons.
- Verified OK: 0/1/2/3/4/5+ photo layouts and cover-crop math; missing-blob skip; empty story/location degradation; 2-line title ellipsis; seeded PRNG determinism; safe-area math (story 220–1700 held for all content paths); object-URL lifecycle (create per session, revoke on close, no leak on stale-session bail); native <dialog> focus trap, Esc via cancel→close, backdrop click, opener focus restore; format-toggle render caching + in-flight dedupe; AbortError silence; canShare({files}) probe with real File; book flip pagination and inert sweep unaffected by the share button; journal re-render/lightbox teardown unaffected; 44px targets on all share controls.
- Found 1 major: openShare() double-open race — the isOpen/dlg.open guard runs before `await getEntry()` and is not re-checked, so a fast double-click makes the 2nd call bump `session` and throw InvalidStateError from showModal(), stranding the modal on a permanent skeleton with disabled buttons.
- Found 6 minor: ImageBitmap leak on the render-error fallback path; dateline clips long location names past the canvas edge; loadPhotos slices photoIds before filtering missing blobs (under-fills collage); bk-iconbtn resting opacity 0.6 puts the book share glyph below 3:1; journal share pill white-on-44%-scrim can fall to ~3:1 over light photos; generic "Share this page" aria-label indistinguishable across journal chapters.
- No files edited (reviewer is read-only).
