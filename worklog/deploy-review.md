# Deploy-readiness review (Cloudflare Pages) — adversarial reviewer

- 2026-07-12 (skeptic pass) — Refuted the CRLF-in-Location header-injection finding against functions/_middleware.js redirect() (~line 100). Confirmed sanitizeTo (lines 84-88) does NOT strip control chars, and both decode paths (query string via URL/searchParams, form body via URLSearchParams) yield a real `\r\n`. But the fetch-spec Headers constructor rejects CR/LF in a value with a TypeError (reproduced under undici, which matches workerd) — so no header injection is possible. The only redirect() call receiving a raw tainted `to` is the successful-POST path (line 369), which requires the correct SITE_PASSWORD; the unauthenticated bounce (line 383) encodeURIComponent's `to` first. Worst case is a self-inflicted 500 by an already-authenticated victim, not injection. Verdict: not a real defect — pure defense-in-depth nit. real=false.
- 2026-07-12 — Reviewed wrangler.toml, functions/_middleware.js, SETUP-CLOUDFLARE.md, SETUP.md, index.html paths, js/sharecard.js + js/storycard.js (Safari/iOS share flow). Verified clean: wrangler.toml keys are all valid Pages keys (name / pages_build_output_dir / compatibility_date, no worker-only keys); middleware export shape (`export async function onRequest(context)`) is correct and root middleware gates every request; all app HTML/CSS/JS paths are ./relative (no absolute /paths anywhere); the middleware login page is fully self-contained (inline CSS + data-URI favicons, no gated ./css links); share flow guards File constructor, navigator.canShare({files}), dialog support, createImageBitmap and canvas.toBlob fallbacks — navigator.share is called synchronously inside the click gesture as iOS requires; all JS passes `node --check`; SETUP-CLOUDFLARE.md wrangler v4 commands (login / pages project create --production-branch / pages secret put / pages deploy) are accurate. Findings reported: 1 major (deploying repo root with `pages_build_output_dir = "."` will upload any local `.dev.vars`/`.wrangler` artifacts the documented `pages dev` preview flow creates — no Pages ignore mechanism), 3 minor (internal docs/worklogs/legacy/schema uploaded and served; SETUP.md step 4 still points Supabase Site URL at the retired github.io address; unconditional `Secure` cookie breaks the documented local `wrangler pages dev` gate preview in Safari over http). Returned via structured output.
