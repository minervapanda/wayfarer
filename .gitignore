.DS_Store

# Cloudflare Pages: local secrets, dev-server state, and the staged deploy dir
.dev.vars
.wrangler/
dist/

# Local test fixtures (agent-generated JPEGs) — must never land in the deployed site
testdata/

# local secrets — never commit
SUPABASE-SECRETS.local.txt
